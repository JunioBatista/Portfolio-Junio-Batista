import styled from 'styled-components'

export const SkillsMainContainer = styled.section`
.tools-container {
    display: grid;
    grid-template-columns: repeat(5,1fr);
    justify-content: space-around;
    flex-wrap: wrap;
}
`